/* A packrat parser generated by PackCC 1.2.5 */

#ifdef _MSC_VER
#define _CRT_SECURE_NO_WARNINGS
#endif /* _MSC_VER */
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdbool.h>

#ifndef _MSC_VER
#if defined __GNUC__ && defined _WIN32 /* MinGW */
static size_t strnlen(const char *str, size_t maxlen) {
    size_t i;
    for (i = 0; i < maxlen && str[i]; i++);
    return i;
}
#else
#include <unistd.h> /* for strnlen() */
#endif /* defined __GNUC__ && defined _WIN32 */ 
#endif /* _MSC_VER */

#include "./jqp.h"


static int _jqp_get_char(JPQAUX *aux) {
  if (aux->rc || *(aux->buf + aux->pos) == '\0') {
    return -1;
  } else {
    char ch = *(aux->buf + aux->pos++);
    if (ch == '\n') {
      ++aux->line;
      aux->col = 0; 
    }
    ++aux->col;
    return ch;
  }
}

static void _jqp_error(JPQAUX *aux) {
  if (!aux->rc) {
    aux->rc = JQL_ERROR_QUERY_PARSE;
  }
  if (aux->fatal_jmp_set) {
    longjmp(aux->fatal_jmp, 1);
  } else {
    fprintf(stderr, "Query parsing error (JQL_ERROR_QUERY_PARSE)\n");
  }
}

static void *_jqp_malloc(JPQAUX *aux, size_t size) {
  void *ret = malloc(size);
  if (!ret) {
    aux->rc = iwrc_set_errno(IW_ERROR_ALLOC, errno);
    if (aux->fatal_jmp_set) {
      longjmp(aux->fatal_jmp, 1);
    } else {
      fprintf(stderr, "Memory allocation failed (IW_ERROR_ALLOC)\n");
      exit(1);
    }
  }
  return ret;
}

static void *_jqp_realloc(JPQAUX *aux, void *ptr, size_t size) {
  void *ret = realloc(ptr, size);
  if (!ret) {
    aux->rc = iwrc_set_errno(IW_ERROR_ALLOC, errno);
    if (aux->fatal_jmp_set) {
      longjmp(aux->fatal_jmp, 1);
    } else {
      fprintf(stderr, "Memory allocation failed (IW_ERROR_ALLOC)\n");
      exit(1);
    }
  }
  return ret;
}

#define PCC_GETCHAR(auxil) _jqp_get_char(auxil)
#define PCC_ERROR(auxil) _jqp_error(auxil)
#define PCC_MALLOC(auxil, size) _jqp_malloc(auxil, size)
#define PCC_REALLOC(auxil, ptr, size) _jqp_realloc(auxil, ptr, size)

#ifndef PCC_BUFFERSIZE
#define PCC_BUFFERSIZE 256
#endif /* PCC_BUFFERSIZE */

#ifndef PCC_ARRAYSIZE
#define PCC_ARRAYSIZE 2
#endif /* PCC_ARRAYSIZE */

typedef struct pcc_char_array_tag {
    char *buf;
    int max;
    int len;
} pcc_char_array_t;

typedef struct pcc_range_tag {
    int start;
    int end;
} pcc_range_t;

typedef JPQDATA *pcc_value_t;

typedef JPQAUX *pcc_auxil_t;

typedef struct pcc_value_table_tag {
    pcc_value_t *buf;
    int max;
    int len;
} pcc_value_table_t;

typedef struct pcc_value_refer_table_tag {
    pcc_value_t **buf;
    int max;
    int len;
} pcc_value_refer_table_t;

typedef struct pcc_capture_tag {
    pcc_range_t range;
    char *string; /* mutable */
} pcc_capture_t;

typedef struct pcc_capture_table_tag {
    pcc_capture_t *buf;
    int max;
    int len;
} pcc_capture_table_t;

typedef struct pcc_capture_const_table_tag {
    const pcc_capture_t **buf;
    int max;
    int len;
} pcc_capture_const_table_t;

typedef struct pcc_thunk_tag pcc_thunk_t;
typedef struct pcc_thunk_array_tag pcc_thunk_array_t;

typedef void (*pcc_action_t)(jqp_context_t *, pcc_thunk_t *, pcc_value_t *);

typedef enum pcc_thunk_type_tag {
    PCC_THUNK_LEAF,
    PCC_THUNK_NODE,
} pcc_thunk_type_t;

typedef struct pcc_thunk_leaf_tag {
    pcc_value_refer_table_t values;
    pcc_capture_const_table_t capts;
    pcc_capture_t capt0;
    pcc_action_t action;
} pcc_thunk_leaf_t;

typedef struct pcc_thunk_node_tag {
    const pcc_thunk_array_t *thunks; /* just a reference */
    pcc_value_t *value; /* just a reference */
} pcc_thunk_node_t;

typedef union pcc_thunk_data_tag {
    pcc_thunk_leaf_t leaf;
    pcc_thunk_node_t node;
} pcc_thunk_data_t;

struct pcc_thunk_tag {
    pcc_thunk_type_t type;
    pcc_thunk_data_t data;
};

struct pcc_thunk_array_tag {
    pcc_thunk_t **buf;
    int max;
    int len;
};

typedef struct pcc_thunk_chunk_tag {
    pcc_value_table_t values;
    pcc_capture_table_t capts;
    pcc_thunk_array_t thunks;
    int pos;
} pcc_thunk_chunk_t;

typedef struct pcc_lr_entry_tag pcc_lr_entry_t;

typedef enum pcc_lr_answer_type_tag {
    PCC_LR_ANSWER_LR,
    PCC_LR_ANSWER_CHUNK,
} pcc_lr_answer_type_t;

typedef union pcc_lr_answer_data_tag {
    pcc_lr_entry_t *lr;
    pcc_thunk_chunk_t *chunk;
} pcc_lr_answer_data_t;

typedef struct pcc_lr_answer_tag pcc_lr_answer_t;

struct pcc_lr_answer_tag {
    pcc_lr_answer_type_t type;
    pcc_lr_answer_data_t data;
    int pos;
    pcc_lr_answer_t *hold;
};

typedef pcc_thunk_chunk_t *(*pcc_rule_t)(jqp_context_t *);

typedef struct pcc_rule_set_tag {
    pcc_rule_t *buf;
    int max;
    int len;
} pcc_rule_set_t;

typedef struct pcc_lr_head_tag pcc_lr_head_t;

struct pcc_lr_head_tag {
    pcc_rule_t rule;
    pcc_rule_set_t invol;
    pcc_rule_set_t eval;
    pcc_lr_head_t *hold;
};

typedef struct pcc_lr_memo_tag {
    pcc_rule_t rule;
    pcc_lr_answer_t *answer;
} pcc_lr_memo_t;

typedef struct pcc_lr_memo_map_tag {
    pcc_lr_memo_t *buf;
    int max;
    int len;
} pcc_lr_memo_map_t;

typedef struct pcc_lr_table_entry_tag {
    pcc_lr_head_t *head; /* just a reference */
    pcc_lr_memo_map_t memos;
    pcc_lr_answer_t *hold_a;
    pcc_lr_head_t *hold_h;
} pcc_lr_table_entry_t;

typedef struct pcc_lr_table_tag {
    pcc_lr_table_entry_t **buf;
    int max;
    int len;
} pcc_lr_table_t;

struct pcc_lr_entry_tag {
    pcc_rule_t rule;
    pcc_thunk_chunk_t *seed; /* just a reference */
    pcc_lr_head_t *head; /* just a reference */
};

typedef struct pcc_lr_stack_tag {
    pcc_lr_entry_t **buf;
    int max;
    int len;
} pcc_lr_stack_t;

struct jqp_context_tag {
    int pos;
    pcc_char_array_t buffer;
    pcc_lr_table_t lrtable;
    pcc_lr_stack_t lrstack;
    pcc_auxil_t auxil;
};

#ifndef PCC_ERROR
#define PCC_ERROR(auxil) pcc_error()
static void pcc_error(void) {
    fprintf(stderr, "Syntax error\n");
    exit(1);
}
#endif /* PCC_ERROR */

#ifndef PCC_GETCHAR
#define PCC_GETCHAR(auxil) getchar()
#endif /* PCC_GETCHAR */

#ifndef PCC_MALLOC
#define PCC_MALLOC(auxil, size) pcc_malloc_e(size)
static void *pcc_malloc_e(size_t size) {
    void *p = malloc(size);
    if (p == NULL) {
        fprintf(stderr, "Out of memory\n");
        exit(1);
    }
    return p;
}
#endif /* PCC_MALLOC */

#ifndef PCC_REALLOC
#define PCC_REALLOC(auxil, ptr, size) pcc_realloc_e(ptr, size)
static void *pcc_realloc_e(void *ptr, size_t size) {
    void *p = realloc(ptr, size);
    if (p == NULL) {
        fprintf(stderr, "Out of memory\n");
        exit(1);
    }
    return p;
}
#endif /* PCC_REALLOC */

#ifndef PCC_FREE
#define PCC_FREE(auxil, ptr) free(ptr)
#endif /* PCC_FREE */

static char *pcc_strndup_e(pcc_auxil_t auxil, const char *str, size_t len) {
    size_t m = strnlen(str, len);
    char *s = (char *)PCC_MALLOC(auxil, m + 1);
    memcpy(s, str, m);
    s[m] = '\0';
    return s;
}

static void pcc_char_array__init(pcc_auxil_t auxil, pcc_char_array_t *array, int max) {
    array->len = 0;
    array->max = max;
    array->buf = (char *)PCC_MALLOC(auxil, array->max);
}

static void pcc_char_array__add(pcc_auxil_t auxil, pcc_char_array_t *array, char ch) {
    if (array->max <= 0) array->max = 1;
    while (array->max <= array->len) array->max <<= 1;
    array->buf = (char *)PCC_REALLOC(auxil, array->buf, array->max);
    array->buf[array->len++] = ch;
}

static void pcc_char_array__term(pcc_auxil_t auxil, pcc_char_array_t *array) {
    PCC_FREE(auxil, array->buf);
}

static void pcc_value_table__init(pcc_auxil_t auxil, pcc_value_table_t *table, int max) {
    table->len = 0;
    table->max = max;
    table->buf = (pcc_value_t *)PCC_MALLOC(auxil, table->max * sizeof(pcc_value_t));
}

static void pcc_value_table__resize(pcc_auxil_t auxil, pcc_value_table_t *table, int len) {
    if (table->max < len) {
        if (table->max <= 0) table->max = 1;
        while (table->max < len) table->max <<= 1;
        table->buf = (pcc_value_t *)PCC_REALLOC(auxil, table->buf, table->max * sizeof(pcc_value_t));
    }
    table->len = len;
}

static void pcc_value_table__term(pcc_auxil_t auxil, pcc_value_table_t *table) {
    PCC_FREE(auxil, table->buf);
}

static void pcc_value_refer_table__init(pcc_auxil_t auxil, pcc_value_refer_table_t *table, int max) {
    table->len = 0;
    table->max = max;
    table->buf = (pcc_value_t **)PCC_MALLOC(auxil, table->max * sizeof(pcc_value_t *));
}

static void pcc_value_refer_table__resize(pcc_auxil_t auxil, pcc_value_refer_table_t *table, int len) {
    int i;
    if (table->max < len) {
        if (table->max <= 0) table->max = 1;
        while (table->max < len) table->max <<= 1;
        table->buf = (pcc_value_t **)PCC_REALLOC(auxil, table->buf, table->max * sizeof(pcc_value_t *));
    }
    for (i = table->len; i < len; i++) table->buf[i] = NULL;
    table->len = len;
}

static void pcc_value_refer_table__term(pcc_auxil_t auxil, pcc_value_refer_table_t *table) {
    PCC_FREE(auxil, table->buf);
}

static void pcc_capture_table__init(pcc_auxil_t auxil, pcc_capture_table_t *table, int max) {
    table->len = 0;
    table->max = max;
    table->buf = (pcc_capture_t *)PCC_MALLOC(auxil, table->max * sizeof(pcc_capture_t));
}

static void pcc_capture_table__resize(pcc_auxil_t auxil, pcc_capture_table_t *table, int len) {
    int i;
    for (i = table->len - 1; i >= len; i--) PCC_FREE(auxil, table->buf[i].string);
    if (table->max < len) {
        if (table->max <= 0) table->max = 1;
        while (table->max < len) table->max <<= 1;
        table->buf = (pcc_capture_t *)PCC_REALLOC(auxil, table->buf, table->max * sizeof(pcc_capture_t));
    }
    for (i = table->len; i < len; i++) {
        table->buf[i].range.start = 0;
        table->buf[i].range.end = 0;
        table->buf[i].string = NULL;
    }
    table->len = len;
}

static void pcc_capture_table__term(pcc_auxil_t auxil, pcc_capture_table_t *table) {
    int i;
    for (i = table->len - 1; i >= 0; i--) PCC_FREE(auxil, table->buf[i].string);
    PCC_FREE(auxil, table->buf);
}

static void pcc_capture_const_table__init(pcc_auxil_t auxil, pcc_capture_const_table_t *table, int max) {
    table->len = 0;
    table->max = max;
    table->buf = (const pcc_capture_t **)PCC_MALLOC(auxil, table->max * sizeof(const pcc_capture_t *));
}

static void pcc_capture_const_table__resize(pcc_auxil_t auxil, pcc_capture_const_table_t *table, int len) {
    int i;
    if (table->max < len) {
        if (table->max <= 0) table->max = 1;
        while (table->max < len) table->max <<= 1;
        table->buf = (const pcc_capture_t **)PCC_REALLOC(auxil, (pcc_capture_t **)table->buf, table->max * sizeof(const pcc_capture_t *));
    }
    for (i = table->len; i < len; i++) table->buf[i] = NULL;
    table->len = len;
}

static void pcc_capture_const_table__term(pcc_auxil_t auxil, pcc_capture_const_table_t *table) {
    PCC_FREE(auxil, (pcc_capture_t **)table->buf);
}

static pcc_thunk_t *pcc_thunk__create_leaf(pcc_auxil_t auxil, pcc_action_t action, int valuec, int captc) {
    pcc_thunk_t *thunk = (pcc_thunk_t *)PCC_MALLOC(auxil, sizeof(pcc_thunk_t));
    thunk->type = PCC_THUNK_LEAF;
    pcc_value_refer_table__init(auxil, &thunk->data.leaf.values, valuec);
    pcc_value_refer_table__resize(auxil, &thunk->data.leaf.values, valuec);
    pcc_capture_const_table__init(auxil, &thunk->data.leaf.capts, captc);
    pcc_capture_const_table__resize(auxil, &thunk->data.leaf.capts, captc);
    thunk->data.leaf.capt0.range.start = 0;
    thunk->data.leaf.capt0.range.end = 0;
    thunk->data.leaf.capt0.string = NULL;
    thunk->data.leaf.action = action;
    return thunk;
}

static pcc_thunk_t *pcc_thunk__create_node(pcc_auxil_t auxil, const pcc_thunk_array_t *thunks, pcc_value_t *value) {
    pcc_thunk_t *thunk = (pcc_thunk_t *)PCC_MALLOC(auxil, sizeof(pcc_thunk_t));
    thunk->type = PCC_THUNK_NODE;
    thunk->data.node.thunks = thunks;
    thunk->data.node.value = value;
    return thunk;
}

static void pcc_thunk__destroy(pcc_auxil_t auxil, pcc_thunk_t *thunk) {
    if (thunk == NULL) return;
    switch (thunk->type) {
    case PCC_THUNK_LEAF:
        PCC_FREE(auxil, thunk->data.leaf.capt0.string);
        pcc_capture_const_table__term(auxil, &thunk->data.leaf.capts);
        pcc_value_refer_table__term(auxil, &thunk->data.leaf.values);
        break;
    case PCC_THUNK_NODE:
        break;
    default: /* unknown */
        break;
    }
    PCC_FREE(auxil, thunk);
}

static void pcc_thunk_array__init(pcc_auxil_t auxil, pcc_thunk_array_t *array, int max) {
    array->len = 0;
    array->max = max;
    array->buf = (pcc_thunk_t **)PCC_MALLOC(auxil, array->max * sizeof(pcc_thunk_t *));
}

static void pcc_thunk_array__add(pcc_auxil_t auxil, pcc_thunk_array_t *array, pcc_thunk_t *thunk) {
    if (array->max <= 0) array->max = 1;
    while (array->max <= array->len) array->max <<= 1;
    array->buf = (pcc_thunk_t **)PCC_REALLOC(auxil, array->buf, array->max * sizeof(pcc_thunk_t *));
    array->buf[array->len++] = thunk;
}

static void pcc_thunk_array__revert(pcc_auxil_t auxil, pcc_thunk_array_t *array, int len) {
    if (array->len > len) {
        int i;
        for (i = array->len - 1; i >= len; i--) pcc_thunk__destroy(auxil, array->buf[i]);
        array->len = len;
    }
}

static void pcc_thunk_array__term(pcc_auxil_t auxil, pcc_thunk_array_t *array) {
    int i;
    for (i = array->len - 1; i >= 0; i--) pcc_thunk__destroy(auxil, array->buf[i]);
    PCC_FREE(auxil, array->buf);
}

static pcc_thunk_chunk_t *pcc_thunk_chunk__create(pcc_auxil_t auxil) {
    pcc_thunk_chunk_t *chunk = (pcc_thunk_chunk_t *)PCC_MALLOC(auxil, sizeof(pcc_thunk_chunk_t));
    pcc_value_table__init(auxil, &chunk->values, PCC_ARRAYSIZE);
    pcc_capture_table__init(auxil, &chunk->capts, PCC_ARRAYSIZE);
    pcc_thunk_array__init(auxil, &chunk->thunks, PCC_ARRAYSIZE);
    chunk->pos = 0;
    return chunk;
}

static void pcc_thunk_chunk__destroy(pcc_auxil_t auxil, pcc_thunk_chunk_t *chunk) {
    if (chunk == NULL) return;
    pcc_thunk_array__term(auxil, &chunk->thunks);
    pcc_capture_table__term(auxil, &chunk->capts);
    pcc_value_table__term(auxil, &chunk->values);
    PCC_FREE(auxil, chunk);
}

static void pcc_rule_set__init(pcc_auxil_t auxil, pcc_rule_set_t *set, int max) {
    set->len = 0;
    set->max = max;
    set->buf = (pcc_rule_t *)PCC_MALLOC(auxil, set->max * sizeof(pcc_rule_t));
}

static int pcc_rule_set__index(pcc_auxil_t auxil, const pcc_rule_set_t *set, pcc_rule_t rule) {
    int i;
    for (i = 0; i < set->len; i++) {
        if (set->buf[i] == rule) return i;
    }
    return -1;
}

static bool pcc_rule_set__add(pcc_auxil_t auxil, pcc_rule_set_t *set, pcc_rule_t rule) {
    int i = pcc_rule_set__index(auxil, set, rule);
    if (i >= 0) return false;
    if (set->max <= 0) set->max = 1;
    while (set->max <= set->len) set->max <<= 1;
    set->buf = (pcc_rule_t *)PCC_REALLOC(auxil, set->buf, set->max * sizeof(pcc_rule_t));
    set->buf[set->len++] = rule;
    return true;
}

static bool pcc_rule_set__remove(pcc_auxil_t auxil, pcc_rule_set_t *set, pcc_rule_t rule) {
    int i = pcc_rule_set__index(auxil, set, rule);
    if (i < 0) return false;
    memmove(set->buf + i, set->buf + (i + 1), (set->len - (i + 1)) * sizeof(pcc_rule_t));
    return true;
}

static void pcc_rule_set__clear(pcc_auxil_t auxil, pcc_rule_set_t *set) {
    set->len = 0;
}

static void pcc_rule_set__copy(pcc_auxil_t auxil, pcc_rule_set_t *set, const pcc_rule_set_t *src) {
    int i;
    pcc_rule_set__clear(auxil, set);
    for (i = 0; i < src->len; i++) {
        pcc_rule_set__add(auxil, set, src->buf[i]);
    }
}

static void pcc_rule_set__term(pcc_auxil_t auxil, pcc_rule_set_t *set) {
    PCC_FREE(auxil, set->buf);
}

static pcc_lr_head_t *pcc_lr_head__create(pcc_auxil_t auxil, pcc_rule_t rule) {
    pcc_lr_head_t *head = (pcc_lr_head_t *)PCC_MALLOC(auxil, sizeof(pcc_lr_head_t));
    head->rule = rule;
    pcc_rule_set__init(auxil, &head->invol, PCC_ARRAYSIZE);
    pcc_rule_set__init(auxil, &head->eval, PCC_ARRAYSIZE);
    head->hold = NULL;
    return head;
}

static void pcc_lr_head__destroy(pcc_auxil_t auxil, pcc_lr_head_t *head) {
    if (head == NULL) return;
    pcc_lr_head__destroy(auxil, head->hold);
    pcc_rule_set__term(auxil, &head->eval);
    pcc_rule_set__term(auxil, &head->invol);
    PCC_FREE(auxil, head);
}

static void pcc_lr_entry__destroy(pcc_auxil_t auxil, pcc_lr_entry_t *lr);

static pcc_lr_answer_t *pcc_lr_answer__create(pcc_auxil_t auxil, pcc_lr_answer_type_t type, int pos) {
    pcc_lr_answer_t *answer = (pcc_lr_answer_t *)PCC_MALLOC(auxil, sizeof(pcc_lr_answer_t));
    answer->type = type;
    answer->pos = pos;
    answer->hold = NULL;
    switch (answer->type) {
    case PCC_LR_ANSWER_LR:
        answer->data.lr = NULL;
        break;
    case PCC_LR_ANSWER_CHUNK:
        answer->data.chunk = NULL;
        break;
    default: /* unknown */
        PCC_FREE(auxil, answer);
        answer = NULL;
    }
    return answer;
}

static void pcc_lr_answer__set_chunk(pcc_auxil_t auxil, pcc_lr_answer_t *answer, pcc_thunk_chunk_t *chunk) {
    pcc_lr_answer_t *a = pcc_lr_answer__create(auxil, answer->type, answer->pos);
    switch (answer->type) {
    case PCC_LR_ANSWER_LR:
        a->data.lr = answer->data.lr;
        break;
    case PCC_LR_ANSWER_CHUNK:
        a->data.chunk = answer->data.chunk;
        break;
    default: /* unknown */
        break;
    }
    a->hold = answer->hold;
    answer->hold = a;
    answer->type = PCC_LR_ANSWER_CHUNK;
    answer->data.chunk = chunk;
}

static void pcc_lr_answer__destroy(pcc_auxil_t auxil, pcc_lr_answer_t *answer) {
    if (answer == NULL) return;
    pcc_lr_answer__destroy(auxil, answer->hold);
    switch (answer->type) {
    case PCC_LR_ANSWER_LR:
        pcc_lr_entry__destroy(auxil, answer->data.lr);
        break;
    case PCC_LR_ANSWER_CHUNK:
        pcc_thunk_chunk__destroy(auxil, answer->data.chunk);
        break;
    default: /* unknown */
        break;
    }
    PCC_FREE(auxil, answer);
}

static void pcc_lr_memo_map__init(pcc_auxil_t auxil, pcc_lr_memo_map_t *map, int max) {
    map->len = 0;
    map->max = max;
    map->buf = (pcc_lr_memo_t *)PCC_MALLOC(auxil, map->max * sizeof(pcc_lr_memo_t));
}

static int pcc_lr_memo_map__index(pcc_auxil_t auxil, pcc_lr_memo_map_t *map, pcc_rule_t rule) {
    int i;
    for (i = 0; i < map->len; i++) {
        if (map->buf[i].rule == rule) return i;
    }
    return -1;
}

static void pcc_lr_memo_map__put(pcc_auxil_t auxil, pcc_lr_memo_map_t *map, pcc_rule_t rule, pcc_lr_answer_t *answer) {
    int i = pcc_lr_memo_map__index(auxil, map, rule);
    if (i >= 0) {
        pcc_lr_answer__destroy(auxil, map->buf[i].answer);
        map->buf[i].answer = answer;
    }
    else {
        if (map->max <= 0) map->max = 1;
        while (map->max <= map->len) map->max <<= 1;
        map->buf = (pcc_lr_memo_t *)PCC_REALLOC(auxil, map->buf, map->max * sizeof(pcc_lr_memo_t));
        map->buf[map->len].rule = rule;
        map->buf[map->len].answer = answer;
        map->len++;
    }
}

static pcc_lr_answer_t *pcc_lr_memo_map__get(pcc_auxil_t auxil, pcc_lr_memo_map_t *map, pcc_rule_t rule) {
    int i = pcc_lr_memo_map__index(auxil, map, rule);
    return (i >= 0) ? map->buf[i].answer : NULL;
}

static void pcc_lr_memo_map__term(pcc_auxil_t auxil, pcc_lr_memo_map_t *map) {
    int i;
    for (i = map->len - 1; i >= 0; i--) pcc_lr_answer__destroy(auxil, map->buf[i].answer);
    PCC_FREE(auxil, map->buf);
}

static pcc_lr_table_entry_t *pcc_lr_table_entry__create(pcc_auxil_t auxil) {
    pcc_lr_table_entry_t *entry = (pcc_lr_table_entry_t *)PCC_MALLOC(auxil, sizeof(pcc_lr_table_entry_t));
    entry->head = NULL;
    pcc_lr_memo_map__init(auxil, &entry->memos, PCC_ARRAYSIZE);
    entry->hold_a = NULL;
    entry->hold_h = NULL;
    return entry;
}

static void pcc_lr_table_entry__destroy(pcc_auxil_t auxil, pcc_lr_table_entry_t *entry) {
    if (entry == NULL) return;
    pcc_lr_head__destroy(auxil, entry->hold_h);
    pcc_lr_answer__destroy(auxil, entry->hold_a);
    pcc_lr_memo_map__term(auxil, &entry->memos);
    PCC_FREE(auxil, entry);
}

static void pcc_lr_table__init(pcc_auxil_t auxil, pcc_lr_table_t *table, int max) {
    table->len = 0;
    table->max = max;
    table->buf = (pcc_lr_table_entry_t **)PCC_MALLOC(auxil, table->max * sizeof(pcc_lr_table_entry_t *));
}

static void pcc_lr_table__resize(pcc_auxil_t auxil, pcc_lr_table_t *table, int len) {
    int i;
    for (i = table->len - 1; i >= len; i--) pcc_lr_table_entry__destroy(auxil, table->buf[i]);
    if (table->max < len) {
        if (table->max <= 0) table->max = 1;
        while (table->max < len) table->max <<= 1;
        table->buf = (pcc_lr_table_entry_t **)PCC_REALLOC(auxil, table->buf, table->max * sizeof(pcc_lr_table_entry_t *));
    }
    for (i = table->len; i < len; i++) table->buf[i] = NULL;
    table->len = len;
}

static void pcc_lr_table__set_head(pcc_auxil_t auxil, pcc_lr_table_t *table, int index, pcc_lr_head_t *head) {
    if (index >= table->len) pcc_lr_table__resize(auxil, table, index + 1);
    if (table->buf[index] == NULL) table->buf[index] = pcc_lr_table_entry__create(auxil);
    table->buf[index]->head = head;
}

static void pcc_lr_table__hold_head(pcc_auxil_t auxil, pcc_lr_table_t *table, int index, pcc_lr_head_t *head) {
    if (index >= table->len) pcc_lr_table__resize(auxil, table, index + 1);
    if (table->buf[index] == NULL) table->buf[index] = pcc_lr_table_entry__create(auxil);
    head->hold = table->buf[index]->hold_h;
    table->buf[index]->hold_h = head;
}

static void pcc_lr_table__set_answer(pcc_auxil_t auxil, pcc_lr_table_t *table, int index, pcc_rule_t rule, pcc_lr_answer_t *answer) {
    if (index >= table->len) pcc_lr_table__resize(auxil, table, index + 1);
    if (table->buf[index] == NULL) table->buf[index] = pcc_lr_table_entry__create(auxil);
    pcc_lr_memo_map__put(auxil, &table->buf[index]->memos, rule, answer);
}

static void pcc_lr_table__hold_answer(pcc_auxil_t auxil, pcc_lr_table_t *table, int index, pcc_lr_answer_t *answer) {
    if (index >= table->len) pcc_lr_table__resize(auxil, table, index + 1);
    if (table->buf[index] == NULL) table->buf[index] = pcc_lr_table_entry__create(auxil);
    answer->hold = table->buf[index]->hold_a;
    table->buf[index]->hold_a = answer;
}

static pcc_lr_head_t *pcc_lr_table__get_head(pcc_auxil_t auxil, pcc_lr_table_t *table, int index) {
    if (index >= table->len || table->buf[index] == NULL) return NULL;
    return table->buf[index]->head;
}

static pcc_lr_answer_t *pcc_lr_table__get_answer(pcc_auxil_t auxil, pcc_lr_table_t *table, int index, pcc_rule_t rule) {
    if (index >= table->len || table->buf[index] == NULL) return NULL;
    return pcc_lr_memo_map__get(auxil, &table->buf[index]->memos, rule);
}

static void pcc_lr_table__shift(pcc_auxil_t auxil, pcc_lr_table_t *table, int count) {
    int i;
    if (count > table->len) count = table->len;
    for (i = 0; i < count; i++) pcc_lr_table_entry__destroy(auxil, table->buf[i]);
    memmove(table->buf, table->buf + count, (table->len - count) * sizeof(pcc_lr_table_entry_t *));
    table->len -= count;
}

static void pcc_lr_table__term(pcc_auxil_t auxil, pcc_lr_table_t *table) {
    int i;
    for (i = table->len - 1; i >= 0; i--) pcc_lr_table_entry__destroy(auxil, table->buf[i]);
    PCC_FREE(auxil, table->buf);
}

static pcc_lr_entry_t *pcc_lr_entry__create(pcc_auxil_t auxil, pcc_rule_t rule) {
    pcc_lr_entry_t *lr = (pcc_lr_entry_t *)PCC_MALLOC(auxil, sizeof(pcc_lr_entry_t));
    lr->rule = rule;
    lr->seed = NULL;
    lr->head = NULL;
    return lr;
}

static void pcc_lr_entry__destroy(pcc_auxil_t auxil, pcc_lr_entry_t *lr) {
    PCC_FREE(auxil, lr);
}

static void pcc_lr_stack__init(pcc_auxil_t auxil, pcc_lr_stack_t *stack, int max) {
    stack->len = 0;
    stack->max = max;
    stack->buf = (pcc_lr_entry_t **)PCC_MALLOC(auxil, stack->max * sizeof(pcc_lr_entry_t *));
}

static void pcc_lr_stack__push(pcc_auxil_t auxil, pcc_lr_stack_t *stack, pcc_lr_entry_t *lr) {
    if (stack->max <= 0) stack->max = 1;
    while (stack->max <= stack->len) stack->max <<= 1;
    stack->buf = (pcc_lr_entry_t **)PCC_REALLOC(auxil, stack->buf, stack->max * sizeof(pcc_lr_entry_t *));
    stack->buf[stack->len++] = lr;
}

static pcc_lr_entry_t *pcc_lr_stack__pop(pcc_auxil_t auxil, pcc_lr_stack_t *stack) {
    return stack->buf[--stack->len];
}

static void pcc_lr_stack__term(pcc_auxil_t auxil, pcc_lr_stack_t *stack) {
    PCC_FREE(auxil, stack->buf);
}

static jqp_context_t *pcc_context__create(pcc_auxil_t auxil) {
    jqp_context_t *ctx = (jqp_context_t *)PCC_MALLOC(auxil, sizeof(jqp_context_t));
    ctx->pos = 0;
    pcc_char_array__init(auxil, &ctx->buffer, PCC_BUFFERSIZE);
    pcc_lr_table__init(auxil, &ctx->lrtable, PCC_BUFFERSIZE);
    pcc_lr_stack__init(auxil, &ctx->lrstack, PCC_ARRAYSIZE);
    ctx->auxil = auxil;
    return ctx;
}

static void pcc_context__destroy(jqp_context_t *ctx) {
    if (ctx == NULL) return;
    pcc_lr_stack__term(ctx->auxil, &ctx->lrstack);
    pcc_lr_table__term(ctx->auxil, &ctx->lrtable);
    pcc_char_array__term(ctx->auxil, &ctx->buffer);
    PCC_FREE(ctx->auxil, ctx);
}

static int pcc_refill_buffer(jqp_context_t *ctx, int num) {
    int n;
    n = ctx->buffer.len - ctx->pos;
    if (n >= num) return n;
    while (ctx->buffer.len < ctx->pos + num) {
        int c = PCC_GETCHAR(ctx->auxil);
        if (c == EOF) break;
        pcc_char_array__add(ctx->auxil, &ctx->buffer, (char)c);
    }
    return ctx->buffer.len - ctx->pos;
}

static void pcc_commit_buffer(jqp_context_t *ctx) {
    memmove(ctx->buffer.buf, ctx->buffer.buf + ctx->pos, ctx->buffer.len - ctx->pos);
    ctx->buffer.len -= ctx->pos;
    pcc_lr_table__shift(ctx->auxil, &ctx->lrtable, ctx->pos);
    ctx->pos = 0;
}

static const char *pcc_get_capture_string(jqp_context_t *ctx, const pcc_capture_t *capt) {
    if (capt->string == NULL)
        ((pcc_capture_t *)capt)->string =
            pcc_strndup_e(ctx->auxil, ctx->buffer.buf + capt->range.start, capt->range.end - capt->range.start);
    return capt->string;
}

static bool pcc_apply_rule(jqp_context_t *ctx, pcc_rule_t rule, pcc_thunk_array_t *thunks, pcc_value_t *value) {
    static pcc_value_t null;
    pcc_thunk_chunk_t *c = NULL;
    int p = ctx->pos;
    bool b = true;
    pcc_lr_answer_t *a = pcc_lr_table__get_answer(ctx->auxil, &ctx->lrtable, p, rule);
    pcc_lr_head_t *h = pcc_lr_table__get_head(ctx->auxil, &ctx->lrtable, p);
    if (h != NULL) {
        if (a == NULL && rule != h->rule && pcc_rule_set__index(ctx->auxil, &h->invol, rule) < 0) {
            b = false;
            c = NULL;
        }
        else if (pcc_rule_set__remove(ctx->auxil, &h->eval, rule)) {
            b = false;
            c = rule(ctx);
            a = pcc_lr_answer__create(ctx->auxil, PCC_LR_ANSWER_CHUNK, ctx->pos);
            a->data.chunk = c;
            pcc_lr_table__hold_answer(ctx->auxil, &ctx->lrtable, p, a);
        }
    }
    if (b) {
        if (a != NULL) {
            ctx->pos = a->pos;
            switch (a->type) {
            case PCC_LR_ANSWER_LR:
                if (a->data.lr->head == NULL) {
                    a->data.lr->head = pcc_lr_head__create(ctx->auxil, rule);
                    pcc_lr_table__hold_head(ctx->auxil, &ctx->lrtable, p, a->data.lr->head);
                }
                {
                    int i;
                    for (i = ctx->lrstack.len - 1; i >= 0; i--) {
                        if (ctx->lrstack.buf[i]->head == a->data.lr->head) break;
                        ctx->lrstack.buf[i]->head = a->data.lr->head;
                        pcc_rule_set__add(ctx->auxil, &a->data.lr->head->invol, ctx->lrstack.buf[i]->rule);
                    }
                }
                c = a->data.lr->seed;
                break;
            case PCC_LR_ANSWER_CHUNK:
                c = a->data.chunk;
                break;
            default: /* unknown */
                break;
            }
        }
        else {
            pcc_lr_entry_t *e = pcc_lr_entry__create(ctx->auxil, rule);
            pcc_lr_stack__push(ctx->auxil, &ctx->lrstack, e);
            a = pcc_lr_answer__create(ctx->auxil, PCC_LR_ANSWER_LR, p);
            a->data.lr = e;
            pcc_lr_table__set_answer(ctx->auxil, &ctx->lrtable, p, rule, a);
            c = rule(ctx);
            pcc_lr_stack__pop(ctx->auxil, &ctx->lrstack);
            a->pos = ctx->pos;
            if (e->head == NULL) {
                pcc_lr_answer__set_chunk(ctx->auxil, a, c);
            }
            else {
                e->seed = c;
                h = a->data.lr->head;
                if (h->rule != rule) {
                    c = a->data.lr->seed;
                    a = pcc_lr_answer__create(ctx->auxil, PCC_LR_ANSWER_CHUNK, ctx->pos);
                    a->data.chunk = c;
                    pcc_lr_table__hold_answer(ctx->auxil, &ctx->lrtable, p, a);
                }
                else {
                    pcc_lr_answer__set_chunk(ctx->auxil, a, a->data.lr->seed);
                    if (a->data.chunk == NULL) {
                        c = NULL;
                    }
                    else {
                        pcc_lr_table__set_head(ctx->auxil, &ctx->lrtable, p, h);
                        for (;;) {
                            ctx->pos = p;
                            pcc_rule_set__copy(ctx->auxil, &h->eval, &h->invol);
                            c = rule(ctx);
                            if (c == NULL || ctx->pos <= a->pos) break;
                            pcc_lr_answer__set_chunk(ctx->auxil, a, c);
                            a->pos = ctx->pos;
                        }
                        pcc_thunk_chunk__destroy(ctx->auxil, c);
                        pcc_lr_table__set_head(ctx->auxil, &ctx->lrtable, p, NULL);
                        ctx->pos = a->pos;
                        c = a->data.chunk;
                    }
                }
            }
        }
    }
    if (c == NULL) return false;
    if (value == NULL) value = &null;
    memset(value, 0, sizeof(pcc_value_t)); /* in case */
    pcc_thunk_array__add(ctx->auxil, thunks, pcc_thunk__create_node(ctx->auxil, &c->thunks, value));
    return true;
}

static void pcc_do_action(jqp_context_t *ctx, const pcc_thunk_array_t *thunks, pcc_value_t *value) {
    int i;
    for (i = 0; i < thunks->len; i++) {
        pcc_thunk_t *thunk = thunks->buf[i];
        switch (thunk->type) {
        case PCC_THUNK_LEAF:
            thunk->data.leaf.action(ctx, thunk, value);
            break;
        case PCC_THUNK_NODE:
            pcc_do_action(ctx, thunk->data.node.thunks, thunk->data.node.value);
            break;
        default: /* unknown */
            break;
        }
    }
}

static void pcc_action_FILTERPREFIX_0(jqp_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const)__pcc_in->data.leaf.capt0.range.end)
    fprintf(stderr, "P: '%s'\n", _0); 
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_FILTERJOIN_0(jqp_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const)__pcc_in->data.leaf.capt0.range.end)
    fprintf(stderr, "J: '%s'\n", _0); 
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_NODE_0(jqp_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const)__pcc_in->data.leaf.capt0.range.end)
    fprintf(stderr, "N: '%s'\n", _0); 
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_PLACEHOLDER_0(jqp_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const)__pcc_in->data.leaf.capt0.range.end)
    fprintf(stderr, "P: '%s'\n", _0); 
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static void pcc_action_OP_0(jqp_context_t *__pcc_ctx, pcc_thunk_t *__pcc_in, pcc_value_t *__pcc_out) {
#define auxil (__pcc_ctx->auxil)
#define __ (*__pcc_out)
#define _0 pcc_get_capture_string(__pcc_ctx, &__pcc_in->data.leaf.capt0)
#define _0s ((const)__pcc_in->data.leaf.capt0.range.start)
#define _0e ((const)__pcc_in->data.leaf.capt0.range.end)
    fprintf(stderr, "O: '%s'\n", _0); 
#undef _0e
#undef _0s
#undef _0
#undef __
#undef auxil
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_QUERY(jqp_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_FILTER(jqp_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_FILTERPREFIX(jqp_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_FILTERJOIN(jqp_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_NODE(jqp_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_OPNODE(jqp_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_OPPAIR(jqp_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_OPLEFT(jqp_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_NODELEFT(jqp_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_OPRIGHT(jqp_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_PLACEHOLDER(jqp_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_OP(jqp_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_OPJOIN(jqp_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_STRN(jqp_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_OBJJ(jqp_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_ARRJ(jqp_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_PAIRJ(jqp_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_VALJ(jqp_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_STRJ(jqp_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_CHJ(jqp_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_CHP(jqp_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_NUMJ(jqp_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_NUMI(jqp_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_NUMF(jqp_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_NUME(jqp_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_HEX(jqp_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_DIGIT(jqp_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule__(jqp_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule___(jqp_context_t *ctx);
static pcc_thunk_chunk_t *pcc_evaluate_rule_EOF(jqp_context_t *ctx);

static pcc_thunk_chunk_t *pcc_evaluate_rule_QUERY(jqp_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_FILTER, &chunk->thunks, NULL)) goto L0000;
    {
        int p, n, i;
        for (i = 0;; i++) {
            p = ctx->pos;
            n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule___, &chunk->thunks, NULL)) goto L0001;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_FILTERJOIN, &chunk->thunks, NULL)) goto L0001;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule___, &chunk->thunks, NULL)) goto L0001;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_FILTER, &chunk->thunks, NULL)) goto L0001;
        }
    L0001:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_EOF, &chunk->thunks, NULL)) goto L0000;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_FILTER(jqp_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_FILTERPREFIX, &chunk->thunks, NULL)) goto L0001;
L0001:;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NODE, &chunk->thunks, NULL)) goto L0000;
    {
        int i;
        for (i = 0;; i++) {
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NODE, &chunk->thunks, NULL)) goto L0002;
        }
    L0002:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_FILTERPREFIX(jqp_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != '@'
    ) goto L0000;
    ctx->pos++;
    {
        int p1 = ctx->pos;
        int i;
        for (i = 0;; i++) {
            char c;
            if (pcc_refill_buffer(ctx, 1) < 1) goto L0001;
            c = ctx->buffer.buf[ctx->pos];
            if (!(
                (c >= 'a' && c <= 'z') ||
                (c >= 'A' && c <= 'Z') ||
                (c >= '0' && c <= '9')
            )) goto L0001;
            ctx->pos++;
        }
    L0001:;
        if (i < 1) {
            ctx->pos = p1;
            goto L0000;
        }
    }
    {
        pcc_thunk_t *thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_FILTERPREFIX_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->pos;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_FILTERJOIN(jqp_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        {
            if (
                pcc_refill_buffer(ctx, 3) < 3 ||
                ((const char *)(ctx->buffer.buf + ctx->pos))[0] != 'a' ||
                ((const char *)(ctx->buffer.buf + ctx->pos))[1] != 'n' ||
                ((const char *)(ctx->buffer.buf + ctx->pos))[2] != 'd'
            ) goto L0002;
            ctx->pos += 3;
        }
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                ((const char *)(ctx->buffer.buf + ctx->pos))[0] != 'o' ||
                ((const char *)(ctx->buffer.buf + ctx->pos))[1] != 'r'
            ) goto L0003;
            ctx->pos += 2;
        }
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            if (
                pcc_refill_buffer(ctx, 3) < 3 ||
                ((const char *)(ctx->buffer.buf + ctx->pos))[0] != 'n' ||
                ((const char *)(ctx->buffer.buf + ctx->pos))[1] != 'o' ||
                ((const char *)(ctx->buffer.buf + ctx->pos))[2] != 't'
            ) goto L0004;
            ctx->pos += 3;
        }
        goto L0001;
    L0004:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    {
        pcc_thunk_t *thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_FILTERJOIN_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->pos;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_NODE(jqp_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != '/'
    ) goto L0000;
    ctx->pos++;
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STRN, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_OPNODE, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            int p1 = ctx->pos;
            int i;
            for (i = 0;; i++) {
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_CHP, &chunk->thunks, NULL)) goto L0005;
            }
        L0005:;
            if (i < 1) {
                ctx->pos = p1;
                goto L0004;
            }
        }
        goto L0001;
    L0004:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    {
        pcc_thunk_t *thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_NODE_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->pos;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_OPNODE(jqp_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != '['
    ) goto L0000;
    ctx->pos++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_OPPAIR, &chunk->thunks, NULL)) goto L0000;
    {
        int p, n, i;
        for (i = 0;; i++) {
            p = ctx->pos;
            n = chunk->thunks.len;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule___, &chunk->thunks, NULL)) goto L0001;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_OPJOIN, &chunk->thunks, NULL)) goto L0001;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule___, &chunk->thunks, NULL)) goto L0001;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_OPPAIR, &chunk->thunks, NULL)) goto L0001;
        }
    L0001:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != ']'
    ) goto L0000;
    ctx->pos++;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_OPPAIR(jqp_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_OPLEFT, &chunk->thunks, NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_OP, &chunk->thunks, NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_OPRIGHT, &chunk->thunks, NULL)) goto L0000;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_OPLEFT(jqp_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STRN, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NODELEFT, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            int p1 = ctx->pos;
            int i;
            for (i = 0;; i++) {
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_CHP, &chunk->thunks, NULL)) goto L0005;
            }
        L0005:;
            if (i < 1) {
                ctx->pos = p1;
                goto L0004;
            }
        }
        goto L0001;
    L0004:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_NODELEFT(jqp_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != '['
    ) goto L0000;
    ctx->pos++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STRN, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            int p1 = ctx->pos;
            int i;
            for (i = 0;; i++) {
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_CHP, &chunk->thunks, NULL)) goto L0004;
            }
        L0004:;
            if (i < 1) {
                ctx->pos = p1;
                goto L0003;
            }
        }
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_OP, &chunk->thunks, NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_OPRIGHT, &chunk->thunks, NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != ']'
    ) goto L0000;
    ctx->pos++;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_OPRIGHT(jqp_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PLACEHOLDER, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_VALJ, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_PLACEHOLDER(jqp_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != ':'
    ) goto L0000;
    ctx->pos++;
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        {
            int p1 = ctx->pos;
            int i;
            for (i = 0;; i++) {
                char c;
                if (pcc_refill_buffer(ctx, 1) < 1) goto L0003;
                c = ctx->buffer.buf[ctx->pos];
                if (!(
                    (c >= 'a' && c <= 'z') ||
                    (c >= 'A' && c <= 'Z') ||
                    (c >= '0' && c <= '9')
                )) goto L0003;
                ctx->pos++;
            }
        L0003:;
            if (i < 1) {
                ctx->pos = p1;
                goto L0002;
            }
        }
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != '?'
        ) goto L0004;
        ctx->pos++;
        goto L0001;
    L0004:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    {
        pcc_thunk_t *thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_PLACEHOLDER_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->pos;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_OP(jqp_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        {
            int p = ctx->pos;
            int n = chunk->thunks.len;
            {
                if (
                    pcc_refill_buffer(ctx, 3) < 3 ||
                    ((const char *)(ctx->buffer.buf + ctx->pos))[0] != 'n' ||
                    ((const char *)(ctx->buffer.buf + ctx->pos))[1] != 'o' ||
                    ((const char *)(ctx->buffer.buf + ctx->pos))[2] != 't'
                ) goto L0003;
                ctx->pos += 3;
            }
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule___, &chunk->thunks, NULL)) goto L0003;
            goto L0004;
        L0003:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0004:;
        }
        {
            int p = ctx->pos;
            int n = chunk->thunks.len;
            {
                if (
                    pcc_refill_buffer(ctx, 4) < 4 ||
                    ((const char *)(ctx->buffer.buf + ctx->pos))[0] != 'l' ||
                    ((const char *)(ctx->buffer.buf + ctx->pos))[1] != 'i' ||
                    ((const char *)(ctx->buffer.buf + ctx->pos))[2] != 'k' ||
                    ((const char *)(ctx->buffer.buf + ctx->pos))[3] != 'e'
                ) goto L0006;
                ctx->pos += 4;
            }
            goto L0005;
        L0006:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            {
                if (
                    pcc_refill_buffer(ctx, 2) < 2 ||
                    ((const char *)(ctx->buffer.buf + ctx->pos))[0] != 'i' ||
                    ((const char *)(ctx->buffer.buf + ctx->pos))[1] != 'n'
                ) goto L0007;
                ctx->pos += 2;
            }
            goto L0005;
        L0007:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            {
                if (
                    pcc_refill_buffer(ctx, 2) < 2 ||
                    ((const char *)(ctx->buffer.buf + ctx->pos))[0] != 'r' ||
                    ((const char *)(ctx->buffer.buf + ctx->pos))[1] != 'e'
                ) goto L0008;
                ctx->pos += 2;
            }
            goto L0005;
        L0008:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
            goto L0002;
        L0005:;
        }
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                ((const char *)(ctx->buffer.buf + ctx->pos))[0] != '>' ||
                ((const char *)(ctx->buffer.buf + ctx->pos))[1] != '='
            ) goto L0009;
            ctx->pos += 2;
        }
        goto L0001;
    L0009:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                ((const char *)(ctx->buffer.buf + ctx->pos))[0] != '<' ||
                ((const char *)(ctx->buffer.buf + ctx->pos))[1] != '='
            ) goto L0010;
            ctx->pos += 2;
        }
        goto L0001;
    L0010:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            int p = ctx->pos;
            int n = chunk->thunks.len;
            if (
                pcc_refill_buffer(ctx, 1) < 1 ||
                ctx->buffer.buf[ctx->pos] != '!'
            ) goto L0012;
            ctx->pos++;
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0012;
            goto L0013;
        L0012:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        L0013:;
        }
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != '='
        ) goto L0011;
        ctx->pos++;
        goto L0001;
    L0011:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != '>'
        ) goto L0014;
        ctx->pos++;
        goto L0001;
    L0014:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != '<'
        ) goto L0015;
        ctx->pos++;
        goto L0001;
    L0015:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            if (
                pcc_refill_buffer(ctx, 3) < 3 ||
                ((const char *)(ctx->buffer.buf + ctx->pos))[0] != 'n' ||
                ((const char *)(ctx->buffer.buf + ctx->pos))[1] != 'o' ||
                ((const char *)(ctx->buffer.buf + ctx->pos))[2] != 't'
            ) goto L0016;
            ctx->pos += 3;
        }
        goto L0001;
    L0016:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    {
        pcc_thunk_t *thunk = pcc_thunk__create_leaf(ctx->auxil, pcc_action_OP_0, 0, 0);
        thunk->data.leaf.capt0.range.start = chunk->pos;
        thunk->data.leaf.capt0.range.end = ctx->pos;
        pcc_thunk_array__add(ctx->auxil, &chunk->thunks, thunk);
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_OPJOIN(jqp_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        {
            if (
                pcc_refill_buffer(ctx, 3) < 3 ||
                ((const char *)(ctx->buffer.buf + ctx->pos))[0] != 'a' ||
                ((const char *)(ctx->buffer.buf + ctx->pos))[1] != 'n' ||
                ((const char *)(ctx->buffer.buf + ctx->pos))[2] != 'd'
            ) goto L0002;
            ctx->pos += 3;
        }
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            if (
                pcc_refill_buffer(ctx, 2) < 2 ||
                ((const char *)(ctx->buffer.buf + ctx->pos))[0] != 'o' ||
                ((const char *)(ctx->buffer.buf + ctx->pos))[1] != 'r'
            ) goto L0003;
            ctx->pos += 2;
        }
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_STRN(jqp_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != '\"'
    ) goto L0000;
    ctx->pos++;
    {
        int p1 = ctx->pos;
        int i;
        for (i = 0;; i++) {
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_CHJ, &chunk->thunks, NULL)) goto L0001;
        }
    L0001:;
        if (i < 1) {
            ctx->pos = p1;
            goto L0000;
        }
    }
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != '\"'
    ) goto L0000;
    ctx->pos++;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_OBJJ(jqp_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != '{'
    ) goto L0000;
    ctx->pos++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PAIRJ, &chunk->thunks, NULL)) goto L0001;
        {
            int p, n, i;
            for (i = 0;; i++) {
                p = ctx->pos;
                n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0003;
                if (
                    pcc_refill_buffer(ctx, 1) < 1 ||
                    ctx->buffer.buf[ctx->pos] != ','
                ) goto L0003;
                ctx->pos++;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0003;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_PAIRJ, &chunk->thunks, NULL)) goto L0003;
            }
        L0003:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        }
        goto L0002;
    L0001:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
    L0002:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != '}'
    ) goto L0000;
    ctx->pos++;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_ARRJ(jqp_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != '['
    ) goto L0000;
    ctx->pos++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_VALJ, &chunk->thunks, NULL)) goto L0001;
        {
            int p, n, i;
            for (i = 0;; i++) {
                p = ctx->pos;
                n = chunk->thunks.len;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0003;
                if (
                    pcc_refill_buffer(ctx, 1) < 1 ||
                    ctx->buffer.buf[ctx->pos] != ','
                ) goto L0003;
                ctx->pos++;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0003;
                if (!pcc_apply_rule(ctx, pcc_evaluate_rule_VALJ, &chunk->thunks, NULL)) goto L0003;
            }
        L0003:;
            ctx->pos = p;
            pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        }
        goto L0002;
    L0001:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
    L0002:;
    }
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != ']'
    ) goto L0000;
    ctx->pos++;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_PAIRJ(jqp_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STRJ, &chunk->thunks, NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != ':'
    ) goto L0000;
    ctx->pos++;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule__, &chunk->thunks, NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STRJ, &chunk->thunks, NULL)) goto L0000;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_VALJ(jqp_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_STRJ, &chunk->thunks, NULL)) goto L0002;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NUMJ, &chunk->thunks, NULL)) goto L0003;
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_OBJJ, &chunk->thunks, NULL)) goto L0004;
        goto L0001;
    L0004:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_ARRJ, &chunk->thunks, NULL)) goto L0005;
        goto L0001;
    L0005:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            if (
                pcc_refill_buffer(ctx, 4) < 4 ||
                ((const char *)(ctx->buffer.buf + ctx->pos))[0] != 't' ||
                ((const char *)(ctx->buffer.buf + ctx->pos))[1] != 'r' ||
                ((const char *)(ctx->buffer.buf + ctx->pos))[2] != 'u' ||
                ((const char *)(ctx->buffer.buf + ctx->pos))[3] != 'e'
            ) goto L0006;
            ctx->pos += 4;
        }
        goto L0001;
    L0006:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            if (
                pcc_refill_buffer(ctx, 5) < 5 ||
                ((const char *)(ctx->buffer.buf + ctx->pos))[0] != 'f' ||
                ((const char *)(ctx->buffer.buf + ctx->pos))[1] != 'a' ||
                ((const char *)(ctx->buffer.buf + ctx->pos))[2] != 'l' ||
                ((const char *)(ctx->buffer.buf + ctx->pos))[3] != 's' ||
                ((const char *)(ctx->buffer.buf + ctx->pos))[4] != 'e'
            ) goto L0007;
            ctx->pos += 5;
        }
        goto L0001;
    L0007:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            if (
                pcc_refill_buffer(ctx, 4) < 4 ||
                ((const char *)(ctx->buffer.buf + ctx->pos))[0] != 'n' ||
                ((const char *)(ctx->buffer.buf + ctx->pos))[1] != 'u' ||
                ((const char *)(ctx->buffer.buf + ctx->pos))[2] != 'l' ||
                ((const char *)(ctx->buffer.buf + ctx->pos))[3] != 'l'
            ) goto L0008;
            ctx->pos += 4;
        }
        goto L0001;
    L0008:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_STRJ(jqp_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != '\"'
    ) goto L0000;
    ctx->pos++;
    {
        int i;
        for (i = 0;; i++) {
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_CHJ, &chunk->thunks, NULL)) goto L0001;
        }
    L0001:;
    }
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != '\"'
    ) goto L0000;
    ctx->pos++;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_CHJ(jqp_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != '\\'
        ) goto L0002;
        ctx->pos++;
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != '\"'
        ) goto L0002;
        ctx->pos++;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != '\\'
        ) goto L0003;
        ctx->pos++;
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != '\\'
        ) goto L0003;
        ctx->pos++;
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != '\\'
        ) goto L0004;
        ctx->pos++;
        {
            char c;
            if (pcc_refill_buffer(ctx, 1) < 1) goto L0004;
            c = ctx->buffer.buf[ctx->pos];
            if (!(
                c == 'b' ||
                c == 'f' ||
                c == 'n' ||
                c == 'r' ||
                c == 't'
            )) goto L0004;
            ctx->pos++;
        }
        goto L0001;
    L0004:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != '\\'
        ) goto L0005;
        ctx->pos++;
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != 'u'
        ) goto L0005;
        ctx->pos++;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_HEX, &chunk->thunks, NULL)) goto L0005;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_HEX, &chunk->thunks, NULL)) goto L0005;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_HEX, &chunk->thunks, NULL)) goto L0005;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_HEX, &chunk->thunks, NULL)) goto L0005;
        goto L0001;
    L0005:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            int pp = ctx->pos;
            if (
                pcc_refill_buffer(ctx, 1) < 1 ||
                ctx->buffer.buf[ctx->pos] != '\"'
            ) goto L0007;
            ctx->pos++;
            ctx->pos = pp;
            goto L0006;
        L0007:;
            ctx->pos = pp;
        }
        if (pcc_refill_buffer(ctx, 1) < 1) goto L0006;
        ctx->pos++;
        goto L0001;
    L0006:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_CHP(jqp_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != '\\'
        ) goto L0002;
        ctx->pos++;
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != '\\'
        ) goto L0002;
        ctx->pos++;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != '\\'
        ) goto L0003;
        ctx->pos++;
        {
            char c;
            if (pcc_refill_buffer(ctx, 1) < 1) goto L0003;
            c = ctx->buffer.buf[ctx->pos];
            if (!(
                c == 'b' ||
                c == 'f' ||
                c == 'n' ||
                c == 'r' ||
                c == 't'
            )) goto L0003;
            ctx->pos++;
        }
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != '\\'
        ) goto L0004;
        ctx->pos++;
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != 'u'
        ) goto L0004;
        ctx->pos++;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_HEX, &chunk->thunks, NULL)) goto L0004;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_HEX, &chunk->thunks, NULL)) goto L0004;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_HEX, &chunk->thunks, NULL)) goto L0004;
        if (!pcc_apply_rule(ctx, pcc_evaluate_rule_HEX, &chunk->thunks, NULL)) goto L0004;
        goto L0001;
    L0004:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            int pp = ctx->pos;
            {
                char c;
                if (pcc_refill_buffer(ctx, 1) < 1) goto L0006;
                c = ctx->buffer.buf[ctx->pos];
                if (!(
                    c == '/' ||
                    c == '\"' ||
                    c == '[' ||
                    c == ']' ||
                    c == '=' ||
                    c == '>' ||
                    c == '<' ||
                    c == '!' ||
                    c == '\t' ||
                    c == '\n' ||
                    c == '\r' ||
                    c == ' '
                )) goto L0006;
                ctx->pos++;
            }
            ctx->pos = pp;
            goto L0005;
        L0006:;
            ctx->pos = pp;
        }
        if (pcc_refill_buffer(ctx, 1) < 1) goto L0005;
        ctx->pos++;
        goto L0001;
    L0005:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_NUMJ(jqp_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != '-'
    ) goto L0001;
    ctx->pos++;
L0001:;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NUMI, &chunk->thunks, NULL)) goto L0000;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NUMF, &chunk->thunks, NULL)) goto L0002;
L0002:;
    if (!pcc_apply_rule(ctx, pcc_evaluate_rule_NUME, &chunk->thunks, NULL)) goto L0003;
L0003:;
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_NUMI(jqp_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    {
        int p = ctx->pos;
        int n = chunk->thunks.len;
        if (
            pcc_refill_buffer(ctx, 1) < 1 ||
            ctx->buffer.buf[ctx->pos] != '0'
        ) goto L0002;
        ctx->pos++;
        goto L0001;
    L0002:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        {
            char c;
            if (pcc_refill_buffer(ctx, 1) < 1) goto L0003;
            c = ctx->buffer.buf[ctx->pos];
            if (!(c >= '1' && c <= '9')) goto L0003;
            ctx->pos++;
        }
        {
            int i;
            for (i = 0;; i++) {
                char c;
                if (pcc_refill_buffer(ctx, 1) < 1) goto L0004;
                c = ctx->buffer.buf[ctx->pos];
                if (!(c >= '0' && c <= '9')) goto L0004;
                ctx->pos++;
            }
        L0004:;
        }
        goto L0001;
    L0003:;
        ctx->pos = p;
        pcc_thunk_array__revert(ctx->auxil, &chunk->thunks, n);
        goto L0000;
    L0001:;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_NUMF(jqp_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    if (
        pcc_refill_buffer(ctx, 1) < 1 ||
        ctx->buffer.buf[ctx->pos] != '.'
    ) goto L0000;
    ctx->pos++;
    {
        int p1 = ctx->pos;
        int i;
        for (i = 0;; i++) {
            char c;
            if (pcc_refill_buffer(ctx, 1) < 1) goto L0001;
            c = ctx->buffer.buf[ctx->pos];
            if (!(c >= '0' && c <= '9')) goto L0001;
            ctx->pos++;
        }
    L0001:;
        if (i < 1) {
            ctx->pos = p1;
            goto L0000;
        }
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_NUME(jqp_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    {
        char c;
        if (pcc_refill_buffer(ctx, 1) < 1) goto L0000;
        c = ctx->buffer.buf[ctx->pos];
        if (!(
            c == 'e' ||
            c == 'E'
        )) goto L0000;
        ctx->pos++;
    }
    {
        char c;
        if (pcc_refill_buffer(ctx, 1) < 1) goto L0001;
        c = ctx->buffer.buf[ctx->pos];
        if (!(
            c == '+' ||
            c == '-'
        )) goto L0001;
        ctx->pos++;
    }
L0001:;
    {
        int p1 = ctx->pos;
        int i;
        for (i = 0;; i++) {
            if (!pcc_apply_rule(ctx, pcc_evaluate_rule_DIGIT, &chunk->thunks, NULL)) goto L0002;
        }
    L0002:;
        if (i < 1) {
            ctx->pos = p1;
            goto L0000;
        }
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_HEX(jqp_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    {
        char c;
        if (pcc_refill_buffer(ctx, 1) < 1) goto L0000;
        c = ctx->buffer.buf[ctx->pos];
        if (!(
            (c >= '0' && c <= '9') ||
            (c >= 'A' && c <= 'F') ||
            (c >= 'a' && c <= 'f')
        )) goto L0000;
        ctx->pos++;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_DIGIT(jqp_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    {
        char c;
        if (pcc_refill_buffer(ctx, 1) < 1) goto L0000;
        c = ctx->buffer.buf[ctx->pos];
        if (!(c >= '0' && c <= '9')) goto L0000;
        ctx->pos++;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule__(jqp_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    {
        int i;
        for (i = 0;; i++) {
            char c;
            if (pcc_refill_buffer(ctx, 1) < 1) goto L0001;
            c = ctx->buffer.buf[ctx->pos];
            if (!(
                c == '\t' ||
                c == '\n' ||
                c == '\r' ||
                c == ' '
            )) goto L0001;
            ctx->pos++;
        }
    L0001:;
    }
    return chunk;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule___(jqp_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    {
        int p1 = ctx->pos;
        int i;
        for (i = 0;; i++) {
            char c;
            if (pcc_refill_buffer(ctx, 1) < 1) goto L0001;
            c = ctx->buffer.buf[ctx->pos];
            if (!(
                c == '\t' ||
                c == '\n' ||
                c == '\r' ||
                c == ' '
            )) goto L0001;
            ctx->pos++;
        }
    L0001:;
        if (i < 1) {
            ctx->pos = p1;
            goto L0000;
        }
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

static pcc_thunk_chunk_t *pcc_evaluate_rule_EOF(jqp_context_t *ctx) {
    pcc_thunk_chunk_t *chunk = pcc_thunk_chunk__create(ctx->auxil);
    chunk->pos = ctx->pos;
    {
        int pp = ctx->pos;
        if (pcc_refill_buffer(ctx, 1) < 1) goto L0001;
        ctx->pos++;
        ctx->pos = pp;
        goto L0000;
    L0001:;
        ctx->pos = pp;
    }
    return chunk;
L0000:;
    pcc_thunk_chunk__destroy(ctx->auxil, chunk);
    return NULL;
}

jqp_context_t *jqp_create(JPQAUX *auxil) {
    return pcc_context__create(auxil);
}

int jqp_parse(jqp_context_t *ctx, JPQDATA **ret) {
    pcc_thunk_array_t thunks;
    pcc_thunk_array__init(ctx->auxil, &thunks, PCC_ARRAYSIZE);
    if (pcc_apply_rule(ctx, pcc_evaluate_rule_QUERY, &thunks, ret))
        pcc_do_action(ctx, &thunks, ret);
    else
        PCC_ERROR(ctx->auxil);
    pcc_commit_buffer(ctx);
    pcc_thunk_array__term(ctx->auxil, &thunks);
    return pcc_refill_buffer(ctx, 1) >= 1;
}

void jqp_destroy(jqp_context_t *ctx) {
    pcc_context__destroy(ctx);
}
